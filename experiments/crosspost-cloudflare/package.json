{
  "name": "@syndroo/crosspost-cloudflare-experiment",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@humanwhocodes/crosspost": "1.0.4"
  },
  "scripts": {
    "types": "wrangler types",
    "check": "npm run types && tsc -p tsconfig.json --noEmit",
    "bundle": "wrangler deploy --dry-run --outdir dist",
    "dev": "wrangler dev"
  }
}
